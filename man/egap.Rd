% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stela.R
\name{egap}
\alias{egap}
\alias{print.egap}
\title{Calculate energy gaps between observations and stable states}
\usage{
egap(b, obs, temp = NULL, r = NULL, iter = NULL, th = NULL, seed = NULL)

\method{print}{egap}(x, digits = 2, ...)
}
\arguments{
\item{b}{A basin object returned by \code{\link[=basin]{basin()}}. The model parameters
\code{alpha} and \code{beta}, as well as the basin-analysis settings, are retrieved
from attributes of this object.}

\item{obs}{A numeric matrix or vector of observed community states, with
rows representing observations and columns representing species.
States must be compatible with the dimensions of \code{alpha} and \code{beta}.}

\item{temp}{Optional temperature parameter used when recalculating
transition barriers for newly discovered stable states. If \code{NULL}, the
value stored in \code{b} is used.}

\item{r}{Optional parameter controlling the transition-barrier search.
If \code{NULL}, the value stored in \code{b} is used.}

\item{iter}{Optional number of iterations used for transition-barrier
estimation. If \code{NULL}, the value stored in \code{b} is used.}

\item{th}{Optional threshold for pruning shallow basins. The value stored
in \code{b} is always used when newly discovered stable states require
re-evaluation.}

\item{seed}{Optional random seed used when recalculating transition
barriers. If \code{NULL}, the seed stored in \code{b} is used.}

\item{x}{An egap object.}

\item{digits}{Digits for printing.}

\item{...}{Additional arguments.}
}
\value{
A list with three components:
\describe{
\item{gap}{A data frame containing the energy gap (\code{gap}), observed-state
energy (\code{energy}), associated stable-state index (\code{ss}), and stable-state
energy (\code{bottom}) for each observation.}
\item{state}{The stable states retained after incorporating the observed
states and re-evaluating transition barriers and basin pruning.}
\item{summary}{A data frame summarizing the retained stable states,
including their energy, basin depth, and basin width. Newly discovered
stable states have \code{NA} for depth and width because these quantities are
not estimated by \code{egap()}.}
}
}
\description{
Calculates the energy gap between each observed community state and the
stable state reached by steepest descent. Observed states are first
assigned to stable states identified during the original basin analysis.
If an observation leads to a previously unidentified stable state, the
stable-state set is expanded, transition barriers are recalculated, and
shallow basins are pruned.
}
\details{
The function returns the energy of each observed state, the energy of its
associated stable state, and the resulting energy gap. It also provides
an updated set of retained stable states and their basin summaries.

For each observation, \code{\link[=stpd]{stpd()}} is used to identify the stable state reached
by steepest descent. The energy gap is calculated as the difference between
the observed-state energy and the energy of this associated stable state.

If all observed states lead to stable states already identified by
\code{\link[=basin]{basin()}}, the original basin results are retained. If new stable states
are discovered, they are added to the original stable-state set, duplicate
states are removed, and transition barriers are recalculated using
\code{\link[=ridge]{ridge()}}. The expanded set is then pruned using \code{\link[=prune]{prune()}} before the
observed states are reassigned to the resulting stable states.
}
\seealso{
\code{\link[=basin]{basin()}}, \code{\link[=stpd]{stpd()}}, \code{\link[=ridge]{ridge()}}, \code{\link[=prune]{prune()}}
}
