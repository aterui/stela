% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stela.R
\name{cmrf}
\alias{cmrf}
\alias{print.cmrf}
\title{Fit regularized regressions for a multivariate response (conditional Markov random fields)}
\usage{
cmrf(
  Y,
  X = NULL,
  family = c("binomial", "poisson", "gaussian"),
  type.measure = "default",
  nfolds = 10,
  grouped = TRUE,
  offset = NULL,
  standardize = TRUE,
  penalty.factor = NULL,
  control = list(),
  lambda.method = c("lambda.min", "lambda.1se"),
  sym.method = "min",
  future.seed = TRUE,
  progress = TRUE,
  ...
)

\method{print}{cmrf}(x, digits = 2, ...)
}
\arguments{
\item{Y}{A matrix or data frame of response variables. Must have the same
number of rows as \code{X}.}

\item{X}{A matrix or data frame of predictor variables.}

\item{family}{A character string specifying the response distribution used
by \code{\link[glmnet:glmnet]{glmnet::glmnet()}}. Defaults to \code{"binomial"}.}

\item{type.measure}{A character string specifying the loss used to evaluate
models during cross-validation. See \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}} for details.}

\item{nfolds}{Number of folds used for cross-validation. Defaults to 10.
See \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}} for details.}

\item{grouped}{Logical; whether to use grouped cross-validation statistics.
Defaults to \code{TRUE}. See \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}} for details.}

\item{offset}{A vector of values that will be used as an offset term.
Defaults to \code{NULL}.}

\item{standardize}{Logical flag for x variable standardization,
prior to fitting the model sequence. See \code{\link[glmnet:glmnet]{glmnet::glmnet()}} for details.}

\item{penalty.factor}{User-defined penalty factors that will be applied to each coefficient.
This is a number that multiplies \code{lambda} to allow differential shrinkage.
See \code{\link[glmnet:glmnet]{glmnet::glmnet()}} for details.}

\item{control}{A named list of algorithm control parameters for \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}},
providing per-call overrides of session defaults set by \code{\link[glmnet:glmnet.control]{glmnet::glmnet.control()}}.
See \code{\link[glmnet:glmnet]{glmnet::glmnet()}} for details.}

\item{lambda.method}{A character string specifying the criterion used to
select the regularization parameter when extracting coefficients.
Must be either \code{"lambda.min"} (default) or \code{"lambda.1se"}.}

\item{sym.method}{A character string specifying the method used to
symmetrize pairwise coefficients. Passed to \code{symmetrize()}.}

\item{future.seed}{Logical; whether to generate reproducible random-number
streams for parallel computation.}

\item{progress}{Logical; whether to show a progress bar.}

\item{...}{Additional arguments.}

\item{x}{A cmrf object.}

\item{digits}{Digits for printing.}
}
\value{
A list with two components: \code{alpha}, a matrix of coefficients
for predictors in \code{X}, and \code{beta}, a symmetric matrix of
pairwise coefficients among response variables. The diagonal of
\code{beta} is set to zero. \code{lambda} is the estimated shrinkage
factors.
Model-fitting warnings are stored as a
\code{"warning"} attribute containing a data frame with the response
index, response name, and warning message.
}
\description{
Fits a cross-validated regularized regression model for each response
variable in \code{Y}, using the predictors in \code{X} and the remaining
response variables as predictors. Coefficients are separated into effects
of the predictors in \code{X} (\code{alpha}) and pairwise effects among
response variables (\code{beta}).
}
